// Stub loader for Orchids visual editor.
// In this local portfolio we don't need it,
// so just return the source unchanged.
module.exports = function componentTaggerLoader(source) {
  return source;
};
